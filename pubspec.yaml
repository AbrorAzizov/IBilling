name: ibilling_test
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  intl:

  cupertino_icons: ^1.0.8
  bloc:
  flutter_bloc:
  equatable:
  dartz:
  get_it:
  go_router:
  flutter_svg: ^2.0.10+1
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/bottom_nav_bar_icons/active/
    - assets/bottom_nav_bar_icons/inactive/
    - assets/app_bar/Ellipse 13.svg
